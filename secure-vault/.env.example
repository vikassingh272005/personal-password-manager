DATABASE_URL=postgres://postgres:postgres@localhost:5432/secure_vault
SESSION_SECRET=change-me-to-a-secure-random-string
PORT=3001
CORS_ORIGINS=http://localhost:3000
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:3000
# Comma-separated emails granted the admin role. Leave empty to make the
# first registered account an admin (dev convenience).
ADMIN_EMAILS=
APP_ENV=development
